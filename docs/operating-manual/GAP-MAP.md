# Mapa de Lacunas × Instrumentos — versão medida

- **Status:** ativo como referência; a **coluna Status é derivada de comando**, nunca escrita à mão.
- **Origem:** o dono trouxe um mapa em 2026-08-11; testado contra o repositório na mesma data, **5 de 6
  células [COBERTO] do original eram falsas** (fast-check, Stryker, Toxiproxy, Playwright não instalados;
  "Bancada AV" removida em `b617d8f1`). Esta versão substitui a original, com cada status acompanhado do
  comando que o prova. Diagnóstico-mãe: `docs/operating-manual/ORACLE-DEFICIT.md`.
- **Regra nº 1 deste documento (aprendida do próprio teste):** célula de status sem comando ao lado é
  opinião, e opinião com cara de medida é a classe de defeito que o `lint-layer-gate.md` já teve (declarava
  5 supressões, existiam 3). Ao editar, rode o comando; se não há comando, o status é `[?]`.

Convenção: **[COBERTO]** instrumento existe e é exercido · **[PARCIAL]** cobre parte · **[PAPEL]**
declarado, sem executável · **[ABERTO]** sem instrumento · **[ORÁCULO]** só validador externo alcança.

---

## Nível 1 — Local (dentro de uma função)

| Lacuna | Instrumento real | Status | Comando que prova |
|---|---|---|---|
| Tratamento de erro incompleto | **Jest** (server, 133 suítes/1590 unit) · **Vitest** (my-app, 29 arq/137) — o original atribuía tudo a Vitest; as invariantes de dinheiro vivem no lado **Jest** | [COBERTO] | `grep '"test"' server/package.json my-app/package.json` |
| Borda de entrada | **DTO Zod `.strict()`** na fronteira + testes de rejeição por DTO (12 de 21 DTOs de accounting com teste próprio). ~~fast-check~~ **não é dependência** — só transitiva de `effect`, zero uso | [PARCIAL] | `grep '"fast-check"' server/package.json my-app/package.json` → vazio; `grep -rE "fc\.(assert\|property)" server/src my-app` → 0 |
| Aritmética / off-by-one monetário | Invariantes explícitas em teste (Σdébito=Σcrédito, centavos int, `MAX_CENTS`) + **mutação MANUAL com `.bak`** — ~~Stryker~~ não instalado. Ressalva OPS-003: mordida prova *observação*, não *captura* | [PARCIAL] | `grep -i stryker server/package.json my-app/package.json` → vazio |

## Nível 2 — Intra-módulo

| Lacuna | Instrumento real | Status | Comando que prova |
|---|---|---|---|
| Código morto / desconectado | ~~Bancada AV~~ (removida `b617d8f1`). Resta: **cbm** (grafo, com ressalva CBM-001 — in-degree subreporta frontend) + **`skill-audit wiring`** (rota/KPI/preset órfãos) | [PARCIAL] | `node .claude/skills/skill-audit/skill-audit.mjs wiring` |
| Invariante quebrada | testes de invariante escritos à mão (sem property-based) | [PARCIAL] | idem fast-check acima |
| Estado inconsistente entre funções | — | [ABERTO] | — |
| **CRM — modal aninhado fecha no 1º clique** — `components/ui/Modal.tsx` fecha por `document mousedown` fora do `modalRef`; cada `Modal` faz portal pro body, então clique no modal filho (Criar Oportunidade / Converter Lead / Proposta / Não compareceu, dentro do Lead360) fecha o pai e desmonta o filho (verificado no browser 2026-09-25). Autorização do dono 2026-09-25 (chat): "autorizo corrigir os bugs 1 e 2" | teste-guarda `my-app/components/ui/__tests__/Modal.nested.test.tsx` › `mousedown num campo do modal filho NÃO fecha o modal pai` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — overlay marcado `data-modal-overlay`; o `mousedown` que cai no overlay de OUTRO modal é ignorado (instrumentação → correção no mesmo diff; verificado em build de produção: formulário preenchível, POST 201, backdrop do filho fecha só o filho) | `cd my-app && npx vitest run components/ui/__tests__/Modal.nested.test.tsx` |

## Nível 3 — Fronteira (contrato entre nós)

| Lacuna | Instrumento real | Status | Comando que prova |
|---|---|---|---|
| Contrato implícito | Zod valida formato em runtime — só o contrato escrito | [PARCIAL] | `grep -rc "safeParse" server/src/controllers/` |
| Evolução assimétrica (B muda schema, A lê o antigo) | **snapshot de shape Zod dos DTOs** (Fase 4 deste plano; teste jest, snapshot comitado, diff obrigatório no PR). Limite: só server-side — o espelho FE é tipo TS à mão, comparar exige codegen (resíduo declarado) | [ABERTO] → [PARCIAL] após F4 | `ls server/src/features/accounting/dtos/__tests__/dtoShapeSnapshot*` |
| **Colisão semântica rebase × gate novo em `main`** — PR #267 C1/C2: `createPayable` multi-item (NF-e) chega aos gates LAC-E (#259) e F-D2 com `inventoryProductRef`/`inventoryQty` `undefined`; `inventoryItems[].productRef` nunca é validado. `tsc` + 2226 unit + 514 integração + CI verdes (mocks). Decisão do dono 2026-09-03: fork (a) gate por item + F-D2 por item agregado | teste-guarda `server/src/features/accounting/services/__tests__/PayableService.test.ts` › `multi-item × gates pós-fork (PR #267 C1/C2, fork (a))` (2 casos, vermelhos em 2026-09-03; **verdes desde #267 `9fbe200f`**, que trouxe instrumentação + fix fork (a) no mesmo squash) | [FECHADO 2026-09-03 #267] — célula dizia `[INSTRUMENTADO]` até 2026-09-17; corrigida rodando o comando ao lado (2 passed) | `cd server && npx jest PayableService.test -t "gates pós-fork"` |
| **Wizard "Entrevista com IA" mudo — FE chama rota protegida sem credencial** — `useAiInterview.ts` faz `fetch` a `POST /dashboard/ai/ChatInterview` sem `Authorization` e sem `credentials`; o prefixo `/api/dashboard` é protegido desde o baseline (`0db39612`), a rota nasceu no R28 (`14ba107d`) já assim → 401 em toda chamada (verificado no browser 2026-09-07). Decisão do dono 2026-09-07: "Corrige o 401 mandando o Bearer no hook" | teste-guarda `my-app/features/interview/hooks/__tests__/useAiInterview.test.tsx` › `manda Authorization: Bearer <auth_token> na saudação e no envio de mensagem` (vermelho antes, VERDE após o fix) | [CORRIGIDO 2026-09-07] — `useAiInterview.ts` manda `Authorization: Bearer <auth_token>` nas 2 chamadas (mesmo diff: instrumentação → correção; sign-off de browser pendente, humano) | `cd my-app && npx vitest run features/interview/hooks/__tests__/useAiInterview.test.tsx` |
| **CRM — hooks leem `meta.schema` de envelope** — `GET /api/dynamic-tables/:id` devolve `{success, data:{schema}}` e o `apiClient` não desembrulha; `useActorNames.ts` e `usePipelineBoard.ts` leem `meta.schema` → `null` → tarefas/notas "não disponíveis", filtro de vendedor e create do pipeline somem (verificado no browser + resposta de rede 2026-09-25). Autorização do dono 2026-09-25 (chat): "autorizo corrigir os bugs 1 e 2" | teste-guarda `my-app/features/crm/hooks/__tests__/schemaEnvelope.test.tsx` (2 casos: useActorNames, usePipelineBoard) | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — os 2 hooks leem `meta.data.schema` (instrumentação → correção no mesmo diff; verificado em build de produção: "+ Novo Lead" e painéis de tarefa/nota voltaram) | `cd my-app && npx vitest run features/crm/hooks/__tests__/schemaEnvelope.test.tsx` |
| **DynamicTable — update parcial reseta campo com `defaultValue` (Zod 4)** — no Zod 4 `.partial()` APLICA o `.default()` de campo ausente; `DynamicTableService.updateTableData` valida com `buildZodSchema(schema).partial()` e faz merge `{...existente, ...validado}` → todo patch parcial volta para o default os campos com `defaultValue` não enviados. Visto ao vivo 2026-09-25 (cópia do dev.db): PUT `{amount}` numa oportunidade Won → status `Open`. Classe: todo preset com `defaultValue` e todo caller que manda patch parcial (`advanceStage`, `advanceOpportunity`, edição inline). Autorização do dono 2026-09-25 (chat): "autorizo instrumentar e corrigir o bug do Zod" | teste-guarda `server/src/features/dynamicTables/services/__tests__/DynamicTableService.partialUpdateDefaults.test.ts` › `campo com defaultValue ausente do payload mantém o valor atual` (vermelho: status Won→Open, priority High→Medium) | [CORRIGIDO 2026-09-25] — `buildZodSchema(schema, applyDefaults)`: o caminho parcial (`validateDataAgainstSchema(..., isPartial)`) monta o schema SEM `.default()`; create segue aplicando default. Verificado no app (build prod, cópia do dev.db): ANTES, PUT `{email}` num lead Lost → `Open`; DEPOIS, PUT parcial e `advanceStage` mantêm `Lost`, create sem status → `Open`. Resíduo: dado já resetado em produção não é recuperável por este fix; `SchemaValidator` (sem importador de produção) tem o mesmo padrão | `cd server && npx jest DynamicTableService.partialUpdateDefaults` |
| Idempotência sob redelivery | testes de chave de idempotência existem por write-path (classe varrida — memória `idempotency-class-fix-discipline`); redelivery *forçado* na fronteira, não | [PARCIAL] | `grep -rln "P2002" server/src --include=*.test.ts` |
| **`deleteTableData` ignora `immutableAfter`/`lifecycle`** — regra declarada no schema ("registro `Paid` é imutável", `scope:'all'`) × caminho real: os Guards 2/3 rodam só em `updateTableData`; o delete roda `beforeDelete` → `deleteConstraints` → soft delete sem consultá-los. Um registro que não pode ser editado pode ser apagado. Contrato `[AC-2.2-4]` já nomeia; sem teste-guarda nem item de fila até 2026-09-21 | [ABERTO] — próximo passo = `sessao-instrumentacao` (teste: `immutableAfter scope:'all'` satisfeito → `deleteTableData` deve lançar) e decisão do dono sobre o fix (guard no delete × `deleteConstraints` RESTRICT no pai) | `awk '/async deleteTableData\(/,/^  }$/' server/src/features/dynamicTables/services/DynamicTableService.ts \| grep -c immutableAfter` → 0 |
| **Padrão 2 commits + reconcile copiado à mão, sem declaração checável** — 18 serviços contábeis repetem o padrão em prosa livre; **8 chamam `.postEntry(` fora do `PostingService` (7 com subrazão, `ExerciseClosingService` só razão) sem o cabeçalho `atomicUntil`** (Contrato `[AC-2.3-2]`, decisão 2026-09-21 que rejeitou o motor de domínio — `ADR-DOMAIN-MOTOR-rejected.md`). Sem declaração, o revisor não tem o que conferir linha a linha e a classe `efeito-irreversivel-antes-do-gate-autoritativo` volta por cópia | nenhum executável; gerador `SVC-008` + revisor `REV-008` cobram em diff novo (design-time) | [PAPEL] — vira [COBERTO] quando `server/src/features/accounting/__tests__/atomicUntil.boundary.test.ts` (PR-B, fila 9) rodar verde | Git Bash: `grep -L "atomicUntil:" $(grep -rlE "\.postEntry\(" server/src/features/*/services --include=*.ts \| grep -v -e __tests__ -e PostingService) \| wc -l` → **8** (2026-09-22); alvo 0 |

## Nível 4 — Runtime sistêmico

| Lacuna | Instrumento real | Status | Comando que prova |
|---|---|---|---|
| Corrida de concorrência | Harness real existe para o razão: `PostingRepository.concurrency.test.ts` (50 escritores, `Promise.all`, SQLite WAL serializa). **Agendamentos (`noOverlap`)**: TOCTOU exposto pela Fase 3 (`NoOverlapConcurrency.integration.test.ts`, `Expected: 1, Received: 5`) e **FECHADO em `93945426`** — re-check in-tx com repo tx-bound + `runSerializedIfNoOverlap` → `withTableWriteLock(table.id)`; o `it.failing` virou `it` | [PARCIAL] | `grep -rln "Promise.all" server/src --include=*concurrency*.test.ts` |
| **`unique`/`compositeUnique` de preset sem gate in-tx** — mesma classe do `noOverlap` acima, ainda aberta: `validateAdvancedRules` (SQL `json_extract`, `countByFieldValue`) roda **antes** de `prisma.$transaction(writeCreate\|writeUpdate)`; dentro só `enforceNoOverlap` é re-checado, e `runSerializedIfNoOverlap` só arma o lock quando há regra `noOverlap`. N writes concorrentes da mesma chave persistem todos (lead/cliente duplicado sob concorrência; idempotência de preset não garantida — Contrato `[AC-2.2-2]`). Achado por leitura do motor em 2026-09-21 ao avaliar "contabilidade em DynamicTableData". Conserto pelo padrão da casa: armar o lock também para `unique`/`compositeUnique` + re-rodar `validateAdvancedRules` in-tx com o repo tx-bound (lembrete: Windows serializa SQLite — só a CI Linux prova o vermelho) | teste-guarda `server/src/features/dynamicTables/services/__tests__/UniqueFieldConcurrency.integration.test.ts` › `8 pedidos SIMULTÂNEOS com a mesma (email, tenant): exatamente um persiste` (instrumentado `it.failing` com `Expected: 1, Received: 8` — 2026-09-23; hoje `it`). Barreira em `findAllDataByTableId` fixa todas as leituras antes de qualquer escrita, então o vermelho não depende de o SO serializar o SQLite. Cobre `compositeUnique`; `unique` de campo (`countByFieldValue`) é o mesmo mecanismo e fica sem caso próprio | **FECHADO 2026-09-23** (branch `claude/gapmap7-correcao`) — `runSerializedIfNoOverlap` arma `withTableWriteLock` também quando há `unique`/`compositeUnique` (inclusive escrita de sistema) + `validateAdvancedRules` re-roda in-tx com o repo tx-bound em `writeCreate`/`writeUpdate`; o `it.failing` virou `it` | `cd server && npm run test:integration -- UniqueFieldConcurrency` |
| Redelivery / rajada / fora de ordem | — (não há canal assíncrono exercido; scheduler é in-process) | [ABERTO] | — |
| Rede degradada | ~~Toxiproxy~~ **não existe no repo** | [ABERTO] | `grep -ri toxiproxy . --include=package.json` → 0 |
| Transação parcial | Parcial via harness de integração: mutações M3 (perna fora da tx trava contra o lock) provaram atomicidade em sites específicos; kill-no-meio sistemático, não. **+ guarda de estado-antes-do-efeito (A1)**: 3 testes-guarda injetam falha no `saveFile` e afirmam que o job não fica `EXPORTED` — instrumentados vermelhos e **FECHADOS em 2026-08-20** (job nasce `PROCESSING` → `EXPORTED` pós-arquivo, `FAILED` no catch; resíduo conhecido: falha do próprio `updateJob(FAILED)` mascara o erro original, mas a linha nunca mente sucesso) | [PARCIAL] | `grep -ln "runTransaction" server/src/features/accounting/services/__tests__/*.integration.test.ts` · `npx jest --selectProjects unit -t "claiming EXPORTED"` |
| Caminho feliz ponta a ponta | ~~Playwright~~ **não instalado**; não há `e2e/`. O que existe: **supertest** (HTTP real, 3 arquivos de controller de accounting) + a varredura de browser de 2026-07-23 foi **sessão de agente, não instrumento** (achou os 2 bugs do §5.2) | [PARCIAL] | `ls playwright.config.* e2e/ 2>/dev/null` → vazio; `grep -l supertest server/package.json` |
| Migração / integridade de dado | smoke-migration-gate: **13 relatórios manuais, zero script** → Fase 2 deste plano escreve `scripts/smoke-migration-gate.mjs` (roda contra cópia do dev.db real; não entra no CI — o dado real não existe lá) | [PAPEL] → [COBERTO] após F2 | `ls docs/accounting/SMOKE-MIGRATION-GATE-*.md \| wc -l` → 13; `ls scripts/smoke*` |

## Nível 5 — Domínio (código correto, factualmente errado)

| Lacuna | Instrumento real | Status | Comando que prova |
|---|---|---|---|
| Regra plausível mas errada | **Oráculo externo** (PVA/contador — Bloco A itens 3/6, **nunca rodados**, abertos >14 dias) | [ORÁCULO] | `ACCOUNTING-MASTER-MAP.md` §Bloco A |
| Compliance (LGPD, fiscal, retenção) | Oráculo externo | [ORÁCULO] | idem |
| **CRM — Won decidido pelo `stageType` do cliente** — `CrmPipelineService.advanceOpportunity` fecha Won/Lost pelo `input.stageType`, não pelo tipo gravado da etapa-destino (Won aceito em etapa `negotiation`, verificado via API 2026-09-25). Autorização do dono 2026-09-25 (chat): "instrumentar todas as outras correções" | teste-guarda `server/src/features/crm/services/__tests__/CrmPipelineService.test.ts` › `stageType do cliente diverge da etapa gravada → NÃO fecha como Won` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — autorização do dono (chat): "autorizo corrigir as etapas de fechamento e o Won pelo servidor"; `advanceOpportunity` lê a etapa-destino gravada (tenant-scoped, `NotFoundError` se alheia) e fecha Won/Lost só pelo `type` dela — `stageType` e `status` do cliente são ignorados. App (cópia do dev.db): `closed_won`+`status:Won` do cliente em etapa `negotiation` → segue Open, sem título | `cd server && npx jest CrmPipelineService.test -t "diverge da etapa"` |
| **CRM — pipeline padrão sem etapas de fechamento** — `LeadsSeedOnUnitPlugin` semeia só init/meeting/proposal/negotiation; sem `closed_won`/`closed_lost` a oportunidade não pode ser Ganha/Perdida pela UI (verificado no dev.db 2026-09-25). Autorização idem | teste-guarda `server/src/features/dynamicTables/rules/plugins/__tests__/LeadsSeedOnUnitPlugin.test.ts` › `semeia etapas de fechamento` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — mesma autorização; seed ganha "Ganho" (`closed_won`, 100%) e "Perdido" (`closed_lost`, 0%). App: unidade nova → 6 etapas; opp movida para "Ganho" sem `stageType` → Won + título R$2.500. Resíduo: só unidade NOVA recebe; pipelines existentes seguem sem etapa de fechamento | `cd server && npx jest LeadsSeedOnUnitPlugin.test` |
| **CRM — oportunidade Ganha continua editável (drift CRM × razão)** — mudar o valor depois do Won deixou o CRM em R$90k e título/razão em R$60k e recarimbou `closedAt` (verificado via API 2026-09-25). Dois caminhos abertos: o preset `crmOpportunities` não declara `immutableAfter` (edição inline genérica) e `CrmPipelineService.advanceOpportunity` grava com `isSystem` (que pula o `immutableAfter`) sem checar estado terminal. Decisão do dono 2026-09-25 (chat): "decide pós-Won: travar" — emenda o `ADR-CRM-AR-SEAM` | teste-guarda `server/src/features/crm/services/__tests__/opportunityTerminalLock.test.ts` (2 casos: caminho genérico, advanceOpportunity); mordida provada: com `immutableAfter { status in [Won, Lost], scope:'all' }` injetado, o caso genérico passa | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — autorização do dono (chat): "autorizo corrigir a trava pós-Won"; preset `crmOpportunities` declara `immutableAfter { status in [Won, Lost], scope:'all' }` + `advanceOpportunity` recusa opp Won/Lost antes de gravar; guard virou `it`. Resíduo: `PresetSyncService` é aditivo-only — tenants JÁ instalados não recebem o `immutableAfter` (caminho genérico segue aberto neles até sync de regra) | `cd server && npx jest opportunityTerminalLock` |
| **PresetSync não propaga regra de schema a tenant já instalado** — `PresetSyncService.syncInstalledTableFromPreset` unia só campos e opções de select; regra nova no preset (`immutableAfter` Won/Lost de `crmOpportunities`) nunca chegava a tabela instalada, e a edição inline genérica seguia aberta nela. Autorização do dono 2026-09-25 (chat): "autorizo corrigir o sync do preset para tenants existentes" | teste-guarda `server/src/features/dynamicTables/__tests__/PresetSyncService.test.ts` › `une as regras immutableAfter do preset que faltam no schema instalado` (vermelho: 0 chamadas de update) | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — sync une `immutableAfter` por igualdade estrutural (acrescenta, nunca remove); verificado no app (build prod, cópia do dev.db): após `sync-preset` a opp Won recusa PUT genérico e `advance-opportunity` (400). Resíduo: `lifecycle`/`deleteConstraints`/`noOverlap`/`compositeUnique` seguem sem sync | `cd server && npx jest PresetSyncService.test -t "immutableAfter"` |
| **CRM — analytics ignora oportunidades** — `CrmAnalyticsService.getAnalytics` só lê `leads`; opp Ganha (portadora de receita/título) não entra em Ganhos/ticket. Decisão do dono 2026-09-25 (AskUserQuestion): "Incluir oportunidades" | teste-guarda `server/src/features/crm/services/__tests__/CrmAnalyticsService.test.ts` › `conta a oportunidade Ganha em wonLeads e no ticket médio` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — autorização do dono (chat): "autorizo corrigir o analytics com oportunidades"; `getAnalytics` roda o `crmConversionProcessor` também sobre `crmOpportunities` (`amount`/`winProbability`) e sobrescreve `wonLeads`/`winRate`/`pipelineValue`/`forecast`/`avgTicket`; sem a tabela instalada segue por leads; guard virou `it`. Resíduo: `avgCycleDays` segue por leads. **Fork pós-`b90587ed` (cards ZERARAM no tenant real: `crmOpportunities` instalada e vazia; antes 12 Won por lead) — decisão do dono 2026-09-25 (AskUserQuestion): "qual o verdadeiro conceito de won? se for apenas opps entao aceitar o zero"** → opção (a) aceitar o zero, condicionada a Won = oportunidade. Evidência da condição (inferida do código, não ratificada em ADR): só a opp Won dispara o seam de receita (`maybeSyncOpportunityWon`, `ADR-CRM-AR-SEAM`); `convertLeadToOpportunity` documenta o lead como pré-qualificação; nenhuma transição de servidor grava `status:Won` num lead (só a edição genérica do select). Resíduo: o preset `leads` ainda oferece `Won` em `status` (CA1 do council CRM 20/07, "rebaixar Lead a pré-qualificação", nunca ratificado) — sem implementação nesta sessão | `cd server && npx jest CrmAnalyticsService.test` |
| **CRM — `advanceStage` decide proposta pelo `stageType` do cliente** — `CrmPipelineService.advanceStage` (leads) cria `leadProposals` quando `input.stageType === 'proposal'`, sem ler o `type` gravado da etapa-destino (mesma classe já corrigida em `advanceOpportunity`). Autorização do dono 2026-09-25 (chat): "instrumentar todas as outras correções" | teste-guarda `server/src/features/crm/services/__tests__/CrmPipelineService.test.ts` › `advanceStage` › `stageType do cliente diverge da etapa gravada → NÃO cria proposta (vale o tipo da etapa)` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — autorização do dono (chat): "autorizo corrigir as 3 lacunas"; `advanceStage` lê a etapa-destino gravada (tenant-scoped, `NotFoundError` se alheia) e só cria proposta se o `type` dela for `proposal`; guard virou `it`. Resíduo `AdvanceStageSchema.stageType` aceito-e-ignorado: fechado na linha seguinte | `cd server && npx jest --selectProjects unit --testPathPatterns CrmPipelineService.test -t "NÃO cria proposta"` |
| **CRM — `AdvanceStageSchema` aceita `stageType` ignorado** — resíduo do fix acima: `advanceStage` passou a decidir pelo `type` gravado da etapa, mas o DTO seguia aceitando `stageType` (param aceito-e-ignorado; `Lead360Modal`, `useCrmPipelineBoard` e `pages/crm/leads/[id].tsx` o enviavam). Autorização do dono 2026-09-25 (chat): "corrige a lacuna do AdvanceStageSchema também" | teste-guarda `server/src/features/crm/dtos/__tests__/CrmPipelineDto.test.ts` › `recusa stageType que advanceStage ignora` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — instrumentação → correção no mesmo diff (vermelho antes: `Expected: false · Received: true`); `AdvanceStageSchema` sem `stageType` + `z.strictObject`; os 3 chamadores e `AdvanceStagePayload` param de enviá-lo; `public/openapi.json` regenerado. `stageType` segue existindo no `usePipelineBoard` como estado de UI (decide a captura de proposta), não vai no payload | `cd server && npx jest --selectProjects unit --testPathPatterns CrmPipelineDto` |
| **CRM — avanço de lead para Reunião Agendada sem captura da data** — `LeadsPlugin` (`server/src/features/dynamicTables/rules/plugins/LeadsPlugin.ts:158-169`, desde o baseline `0db39612`) exige `nextActionAt` futuro ao avançar para etapa `meeting`, e `advanceStage` aceita `meetingAt` (→ `nextActionAt`); mas nenhum dos 3 caminhos de avanço do frontend o envia nem pede a data — `Lead360Modal`, o quadro (`usePipelineBoard` → `useCrmPipelineBoard`) e `pages/crm/leads/[id].tsx` (grep de `meetingAt` = 0). Resultado: avançar lead sem data futura já gravada para "Reunião Agendada" dá 400 "A data/horário da reunião deve ser no futuro." (verificado em build de produção 2026-09-26, lead Pied Piper ME, cópia do dev.db). Registro pedido pelo dono 2026-09-26 (chat): "registra no GAP-MAP a lacuna da reunião no Lead360". Autorização do dono 2026-09-26 (chat): "instrumenta e corrige com captura de data no avanço" | teste-guarda `my-app/features/crm/__tests__/meetingCapture.test.tsx` (3 casos: `Lead360Modal`, página do lead, quadro de leads) | [CORRIGIDO 2026-09-26] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — instrumentação → correção no mesmo diff (vermelho antes: `expected null not to be null` ×2 — sem `input[type=datetime-local]`; quadro chamou `advanceStage` sem `meetingAt`); `MeetingCaptureModal` novo (arquivo novo aprovado pelo dono, AskUserQuestion) — datetime-local, Confirmar só com data futura, envia `meetingAt` ISO; `Lead360Modal` e `pages/crm/leads/[id].tsx` abrem a captura quando a próxima etapa é `meeting`; `usePipelineBoard` ganha `captureMeetingAt` (ligado só em `useCrmPipelineBoard` — oportunidades não têm a regra) com `pendingMeeting`/`confirmMeeting`/`cancelMeeting`; chaves `meeting_capture.*` em pt/en. Build de produção 2026-09-26 (cópia do dev.db): os 3 caminhos → 200 com `nextActionAt` = data digitada (14:30 local → `17:30Z`); Cancelar no quadro devolve o card sem POST; data passada desabilita Confirmar | `cd my-app && npx vitest run features/crm/__tests__/meetingCapture.test.tsx` |
| **CRM — página do lead avança para Proposta Enviada sem captura da proposta** — `LeadsPlugin` (`server/src/features/dynamicTables/rules/plugins/LeadsPlugin.ts:171-183`, desde o baseline `0db39612`) exige `latestProposalAmount` (≥ 0), `latestProposalCurrency` e `latestProposalWinProbability` (0–100) ao avançar para etapa `proposal`; o `Lead360Modal` e o quadro abrem o `ProposalCaptureModal`, mas `pages/crm/leads/[id].tsx` avança direto sem pedir nem enviar `amount`/`currency`/`winProbability` (grep de `proposal`/`amount` na página = 0). Resultado esperado: lead sem proposta gravada → 400 "Informe o valor negociado para avançar para \"Proposta Enviada\"." — **inferido do código**, não reproduzido (no teste de 2026-09-26 o lead Oscorp ME já tinha proposta gravada). Achado adjacente, também inferido: o `ProposalCaptureModal` trata Win % como opcional, mas a regra o exige — lead sem probabilidade já gravada que confirme a captura com Win % em branco deve receber 400 "Informe a probabilidade (0-100%) para avançar." (vale para Lead360 e quadro). Registro pedido pelo dono 2026-09-26 (chat): "registra no GAP-MAP a lacuna da proposta na página do lead" — sem autorização de teste/fix | `my-app/features/crm/__tests__/proposalCapture.test.tsx` — casos `(a) página do lead NÃO avança para proposal sem amount/currency/winProbability` e `(b) ProposalCaptureModal não permite confirmar com Win % vazio` (eram `it.fails`; **guard virou `it`** na correção) | [CORRIGIDO 2026-09-26] — autorização do dono (chat): "autorizo corrigir a lacuna da proposta na página do lead" (instrumentado antes em #396 com "autorizo instrumentar a lacuna da proposta na página do lead"). Fix: `pages/crm/leads/[id].tsx` reusa o `ProposalCaptureModal` (mesmo fluxo do `Lead360Modal`) e envia `amount`/`currency`/`winProbability` no `advanceStage`; `ProposalCaptureModal` ganhou `requireWinProbability` (default `true`) — Win % 0–100 obrigatório nos fluxos de lead; `Opp360Modal` e `OppPipelineBoard` passam `false` porque oportunidade não tem regra que exija Win % (`OpportunitiesModule.ts:100` `required: false`; `CrmOpportunityDto.ts:23` `.optional()`; nenhum plugin de oportunidade em `rules/plugins/`) — decisão do dono 2026-09-26 "conferir a regra de opp primeiro". **Sign-off em build de produção (`withAuth`) é gate humano PENDENTE** — não verificado em browser | `cd my-app && npx vitest run features/crm/__tests__/proposalCapture.test.tsx` |
| **CRM — `AdvanceOpportunityDto` aceita `stageType`/`status` ignorados** — desde o Won-pelo-servidor, `advanceOpportunity` decide só pelo `type` da etapa, mas `AdvanceOpportunitySchema` segue aceitando `stageType` e `status` (param aceito-e-ignorado; `Opp360Modal` ainda os envia). Fix = 400 ou sair do DTO com `.strict()` + `Opp360Modal` — o strip default do Zod não fecha. O `dtoShapeSnapshot` cobre só DTOs de contabilidade (0 menções a CRM): tocar este DTO não passa por gate de snapshot. Autorização idem | teste-guarda `server/src/features/crm/dtos/__tests__/CrmOpportunityDto.test.ts` › `recusa stageType/status que advanceOpportunity ignora` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — mesma autorização; `AdvanceOpportunitySchema` sem `stageType`/`status` e `z.strictObject` (chave desconhecida = 400); `Opp360Modal`, `useOppPipelineBoard` e `AdvanceOpportunityPayload` param de enviá-los; `public/openapi.json` regenerado; guard virou `it` | `cd server && npx jest --selectProjects unit --testPathPatterns CrmOpportunityDto` |
| **CRM — `advanceOpportunity` aceita etapa de outro pipeline** — a etapa-destino é conferida só contra o tenant (`leadStages`), não contra o `pipelineId` da oportunidade: etapa `closed_won` de outro pipeline do mesmo tenant fecha a opp como Won. Autorização idem | teste-guarda `server/src/features/crm/services/__tests__/CrmPipelineService.test.ts` › `advanceOpportunity` › `etapa de outro pipeline do mesmo tenant → recusa, sem update` | [CORRIGIDO 2026-09-25] · **mergeado em `main` `af68ef0f` (#385, 2026-09-26)** — mesma autorização; `advanceOpportunity` recusa (`ValidationError`) etapa cujo `pipelineId` difere do da oportunidade, antes de gravar; guard virou `it` | `cd server && npx jest --selectProjects unit --testPathPatterns CrmPipelineService.test -t "outro pipeline"` |
| **i18n — `next-i18next.config.js` `ns` não lista `crm` nem `accounting`** — **verificado** (lido em `origin/main` `4a282a4c`): `ns: ['common', 'database', 'analytics', 'chatMessages', 'finance_view', 'inventory_view', 'products_view']`. Efeito no SSR (namespaces vazios, UI vivendo dos defaults inline) — **inferido e NÃO sustentado pela leitura**: as páginas passam os namespaces explicitamente a `serverSideTranslations` (`pages/crm/index.tsx:106` `['common','crm']`, `pages/crm/analytics.tsx:53` `['common','crm','analytics']`, `pages/accounting/index.tsx:54` `['common','accounting']`), usam `useTranslation('crm')`/`('accounting')`, e `public/locales/{en,pt}/{crm,accounting}.json` existem (verificado); pela semântica do next-i18next a lista explícita carrega o namespace independente do `ns` do config (**assumido**, não executado). Resta verificar em build de produção se `crm`/`accounting` chegam no `__NEXT_DATA__`; se chegarem, a linha fecha como não-lacuna (só higiene do config). Registro pedido pelo dono 2026-09-26 (chat): "autorizo registrar B e C no GAP-MAP" — sem autorização de teste/fix | sem teste-guarda | [ABERTO] — só registro | — |
| **CRM — valor de pipeline diverge entre visão geral e analytics, e soma moedas** — teste vivo 25/09: visão geral R$ 7,696M × analytics R$ 5,949M; valores USD/EUR somados como R$; 0/80 leads com owner — números **inferidos** (do teste vivo de 25/09, não reproduzidos nesta sessão). Localização provável (**inferida** da leitura, sem execução): visão geral = `my-app/features/crm/hooks/useCrmData.ts:55-74` (`computeKpis`, só `leads`, soma `latestProposalAmount` de todo status ≠ Lost/Disqualified — inclui Won), exibido em `my-app/pages/crm/index.tsx:64`; analytics = `server/src/features/analytics/kpis/crm/CrmConversionProcessor.ts:34-36` (só `status === 'Open'`), sobrescrito por oportunidades em `server/src/features/crm/services/CrmAnalyticsService.ts:94`. Nenhum dos dois trechos lê `latestProposalCurrency` → soma multi-moeda sem conversão. Owner vazio: não localizado. Registro pedido pelo dono 2026-09-26 (chat): "autorizo registrar B e C no GAP-MAP" — sem autorização de teste/fix | sem teste-guarda | [ABERTO] — só registro | — |
| Detector de "regra sem lastro" (censo AV) | **SUSPENSO** — é rodada de auditoria; a regra permanente do `CLAUDE.md` veda aparato novo enquanto houver oráculo do Bloco A aberto >14 dias (hoje 4/4) | [SUSPENSO] | `CLAUDE.md` §⛔ |

## Nível O — Omissão (linha que faltava no original)

> **A classe mais cara não tem onde o código está errado — é onde não há código.** O BUG-1 do
> `ACCOUNTING-MASTER-MAP.md §5.2` (13 eventTypes **faltando** na allowlist → 500+rollback em três
> increments) não cabia em célula nenhuma do mapa original. E não é raro: **17% das faltas reais não
> acoplam a mutante nenhum** (Just et al., FSE 2014), dominadas por algoritmo trocado e código ausente.

| Lacuna | Instrumento real | Status | Comando que prova |
|---|---|---|---|
| **SIG-NFE — importação de NF-e não verifica a assinatura (XMLDSig)** (nó `SIG-NFE`, Fase 2 do `PLANO-POS-CONTADOR-2026-09-23.md`; lacuna medida 23/09 V2, confirmada 26/09 no passo 2.1: `parseNfe` em `server/src/lib/nfe.ts:270` checa DTD/modelo/protocolo/cStat/chave e nunca lê `<Signature>`; `NfeImportService.importPurchase` só chama o parser em `:97`; nenhuma lib XMLDSig no `server/package.json`) — nota com assinatura que não confere com o conteúdo é contabilizada igual | teste-guarda `server/src/controllers/__tests__/nfeController.purchase.signature.integration.test.ts` › `SIG-NFE: NF-e com assinatura que não confere é recusada (400) e nada é escrito` — instrumentado VERMELHO 2026-09-26 (`it.failing`; `Expected: 400 / Received: 201`). Limite declarado: sem XML real assinado (E9 espera D2) nem lib XMLDSig, o cenário é o fixture sintético + `<Signature>` bem-formada que não confere, não assinar-e-adulterar de verdade; o caso "nota sem assinatura" é o fork F-SIG-1 e não é afirmado | [INSTRUMENTADO 2026-09-26] — próximo: BRIEF 2.3 (lib + modo fixture) e F-SIG-1 do dono, depois implementação 2.4 (`it.failing` → `it`) | `cd server && npx jest --selectProjects integration --runInBand --testPathPatterns nfeController.purchase.signature` |
| Emitido sem par declarado (allowlist, registro, mapa) | **Teste de classe**: varre a fonte e cruza emitido-vs-declarado. Exemplar: `auditAllowlistCoverage.test.ts` (pegou 2 casos que o grep manual perdeu). Segundo exemplar: guard de path-count do openapi | [PARCIAL] — o padrão existe, cada nova superfície emitido/declarado precisa do seu | `ls server/src/features/accounting/audit/__tests__/auditAllowlistCoverage.test.ts` |
| Declarado no `PAYLOAD_ALLOWLIST` mas sem teste de *produtor* contra nome de terceiro — achado em auditoria de guardas lista-vs-descoberta (2026-09-09), **instrumentado em 2026-09-09 e RESOLVIDO POR INSPEÇÃO (não intestável — não há mecanismo)**: 12 eventTypes com campo `description`/`reason` (`entry.reversed`, `period.soft_closed`/`hard_closed`/`reopened`, `payable.cancelled`/`payment_cancelled`, `receivable.cancelled`/`receipt_cancelled`, `entry.drafted`/`draft_updated`/`rejected`, `reconciliation.unmatched`) só têm teste genérico de mecanismo, não o teste de produtor que `entry.posted` tem (`auditDescription`, FECHADO 2026-08-20). Verificado nos 5 arquivos produtores: em nenhum dos 12 o `reason`/`description` é **construído** a partir de `supplierName`/`customerName` do registro — é `dto.reason`/`dto.description` repassado intacto do chamador (`PostingService.ts:664`, `PayableService.ts:582,631`, `PeriodService.ts:98,130,167`, `EntryApprovalService.ts:111,154,348`, `ReconciliationService.ts:633`). `entry.posted` via `PayableService`/`ReceivableService` é diferente: `recognitionDescription`/`settlementDescription` (`PayableService.ts:905-978`) **constroem** a string a partir do `payable` — daí o boundary. Sem auto-injeção, um teste de produtor aqui só afirmaria eco tautológico (`reason` sai igual ao que entrou) — classe `comentario-de-teste-afirma-o-que-nao-assere`. Risco residual (operador digitar o nome à mão no campo livre) é o mesmo já aceito para posts manuais de `entry.posted` (`auditCanonical.ts:19`) — não é lacuna de produtor | teste de produtor **não se aplica** (mecanismo inexistente, ver achado). O risco de operador que sobrou **foi mitigado** em `BE-INCR-AUDIT-FREETEXT-MASK` (2026-09-10, `docs/accounting/BE-INCR-AUDIT-FREETEXT-MASK-brief.md`): masker central em `AuditService.append` (`audit/auditFreeTextMask.ts`) troca o nome de `Counterparty` viva por `[counterparty:<id>]` nos 13 eventTypes de campo livre, antes da canonicalização — MELHOR-ESFORÇO declarado, erra nas duas direções (ver `LIMITE DECLARADO` no módulo) | [FECHADO POR INSPEÇÃO] + [MITIGADO 2026-09-10] — achado real distinto do presumido; a decisão de produto que ele apontou shipou no incremento acima | `grep -n "reason: dto.reason" server/src/features/accounting/services/PayableService.ts server/src/features/accounting/services/ReceivableService.ts` → passthrough confirmado nos 4 métodos de cancelamento |
| **"blocked" do reconcile sem `reasonCode` — item some sem rastro** (nó C7r, resíduo da rodada 3 #296): o ordering-gate de `reconcileSaleSettlements` (abertura ausente) e `blocked_missing_paid_with_package_id` de `reconcileSalePackageConsumption` fazem `blocked++` + log + `continue` sem `reportPending`, porque o enum `ReconcilePendingReasonCode` (3 valores) não os cobre. Decisão do dono 2026-09-10 (cédula entrevista §2 resposta 23): **(a)** o enum ganha os 2 códigos e ambos vão para a tabela. Registrado aqui pela ratificação, não por censo | teste-guarda `server/src/jobs/__tests__/reconcilePendingReasonCodes.guard.test.ts` (3 casos: captura em cada site com código próprio aceito pelo enum; códigos distintos; enum = 5) — VERMELHO em 2026-09-11 (`reportPending` recebe 0 chamadas) | [FECHADO 2026-09-11] — `sessao-correcao`: enum ganha `OPENING_ENTRY_MISSING` (transitório) e `MISSING_PAID_WITH_PACKAGE_ID` (poison); os 2 sites chamam `reportPendingSafely`; rescan já despachava os 2 `sourceType`s | `cd server && npx jest --selectProjects unit --testPathPatterns reconcilePendingReasonCodes.guard` (verde) |
| Checagem exigida por invariante e nunca escrita | sem instrumento genérico (é o limite estrutural — mutação não gera mutante do que não existe) | [ABERTO] | — |

## Transversais

| Lacuna | Instrumento real | Status | Comando que prova |
|---|---|---|---|
| Segurança | deny-by-default no auth (RISK-SEC-AUTH-001) + `lib/uploadSecurity` + testes de permissão; ~~semgrep~~ ausente | [PARCIAL] | `grep -i semgrep server/package.json` → vazio |
| Camada / arquitetura | **lint-layer-gate** (`my-app/eslint.gate.config.mjs`, `npm run lint:gate` no CI) + zinc-guard diff-scoped — **instrumentos que o mapa original nem listava** | [PARCIAL] (confinamento prisma/apiClient; não cobre tudo do Contrato §2) | `grep -n "lint:gate" .github/workflows/ci.yml` |
| Configuração de implantação | `dockerCompose.qdrant.test.ts` + `nextPublicEnvWiring.test.ts` — nasceram da triagem R1-R3; antes deles, **nenhum arquivo do repo lia o compose** | [PARCIAL] | `ls server/src/config/__tests__/dockerCompose.qdrant.test.ts` |
| Observabilidade / falha silenciosa | logger JSON estruturado (133 `logger.error` em 47 arquivos não-teste — o 134/48 contava 1 teste) + **sink de arquivo NDJSON** (`server/logs/errors-YYYY-MM-DD.ndjson`, error+warn, retenção 14d), lido por `npm run logs:errors`. **Agrega, não alerta**; `Metrics` continua logger disfarçado | [ABERTO] → [PARCIAL] | `cd server && npm run logs:errors` |

---

## Backtest de completude — o corpus real contra as células

Método: os defeitos de produto **reais** deste repositório (2 do §5.2, 8 triados pela bancada —
recuperáveis via `git show b617d8f1:docs/audit/TRIAGEM-*.json` — e o #176), classificados nas células
desta versão do mapa. **Contaminação declarada:** o mapa conheceu estes defeitos; isto mede **ajuste
(completude)**, não predição.

| Defeito real | Célula | Cabe? |
|---|---|---|
| BUG-1 §5.2 — 13 eventTypes fora da allowlist | **Nível O** (omissão) | só depois da linha nova |
| BUG-2 §5.2 — FE descarta `costOfGoodsSold` que a API devolve | Nível 3 · evolução assimétrica | ✅ caso de manual |
| #176 — editar rascunho apaga etiquetas (read não devolvia o que o write destrói) | Nível 3 · contrato implícito read↔write | ✅ com nota |
| `analyticsdefs-sem-parse-runtime` (AV-L1) | Nível 3 · contrato implícito (fronteira HTTP sem parse) | ✅ |
| `strict-de-dto-neutralizado` (AV-R8) | Nível 3 · contrato implícito (o `.strict()` nunca vê o query inteiro) | ✅ |
| `retry-409-reenvia-versao-velha` (AV-R8) | Nível 2 · estado inconsistente (o `action` capturado sobrevive ao refetch) | ✅ |
| `lead360-dateonly-utc-shift` (AV-L1) | Nível 1 · borda de entrada (classe de 7 sites) | ✅ |
| `qdrant-publicado-sem-chave` (R1) | **Transversal · configuração de implantação** | só depois da linha nova |
| `compose-NEXT_PUBLIC-nome-divergente` (R1) | idem | idem |
| `tres-imports-sem-declaracao` (R1) | idem (manifesto) | idem |
| `documentscontroller-bypassa-factory` (AV-L1) | **Transversal · camada/arquitetura** | só depois da linha nova |

**Resultado: 5 de 11 não cabiam no mapa original** (1 omissão + 3 configuração/manifesto + 1 camada).
As três linhas novas desta versão (Nível O, config de implantação, camada) fecham os cinco. A classe
"configuração" ausente bate com a literatura: Mäntylä & Lassenius acharam **zero** defeitos de
suporte/configuração em 759 achados de revisão — é a classe que ninguém enxerga sem instrumento próprio.

**E uma correção contra a fila do mapa original:** ele apostava que nível 3 é o buraco mais valioso com
0 evidência local; o backtest dá **4 de 11 no nível 3** — a aposta é plausível, mas o corpus também dá
**4 de 11 em configuração/camada**, que o original nem listava.

## Predição pré-registrada — 2026-08-11

> **Aposta do mapa (registrada antes dos dados):** dos próximos **10** defeitos de produto reais
> encontrados neste repositório (por qualquer via — teste novo, oráculo, uso), **≥4 serão Nível 3**
> (contrato de fronteira, incluindo read↔write e FE↔BE).
>
> **Baseline no corpus retroativo:** 4 de 11.
> **Falsificador:** manter a lista dos próximos 10 nesta seção; se <4 forem nível 3, a fila de
> prioridade deste mapa está errada e o item "contrato de fronteira" desce.
>
> Registro (preencher conforme aparecem):
>
> 1. **2026-08-11 — TOCTOU do `noOverlap` / double-booking de agendamento — Nível 4 (corrida de
>    concorrência).** `enforceNoOverlap` decidia por scan na fase de validação, fora da transação do
>    insert; N pedidos que lessem antes do primeiro commit persistiam todos. Achado pelo harness de
>    concorrência da Fase 3 (`NoOverlapConcurrency.integration.test.ts`), que mediu `Expected: 1,
>    Received: 5`.
>
> 2. **2026-08-15 — estado de sucesso gravado antes do efeito (A1 da auditoria) — Nível 4 (transação
>    parcial).** `createJob({status:'EXPORTED'})` antes de `saveFile`, em 3 sítios (ECD, ECF e o
>    export CSV/XLSX, este último fora do relatório original); `'FAILED'` não é escrito por nenhum.
>    Achado por leitura dirigida (AV-06), instrumentado em 2026-08-20 com 3 testes-guarda vermelhos.
>
> 3. **2026-08-15 — nome do fornecedor na trilha imutável (A2 da auditoria) — Nível 3 (contrato
>    implícito).** O allowlist declara em comentário "NEVER the supplier name" e o `payable.created`
>    obedece, mas `description` é allowlistado em `entry.posted` e o subrazão monta a descrição com o
>    nome — regra escrita em prosa não é executável. Fork (b) ratificado pelo dono em 2026-08-20;
>    a pausa inicial (a mesma `description` é o HISTÓRICO do I250 da ECD) foi resolvida na
>    implementação pela **variante de fronteira**: campo interno `auditDescription` sanitizado só no
>    payload de `entry.posted` — a row (e o I250) mantém o nome; fora do Zod de propósito (cliente
>    não seta). **FECHADO 2026-08-20** com guardas em PayableService/ReceivableService/PostingService;
>    review independente PASS (ver `docs/accounting/TRIAGEM-AUDIT-2026-08-15.md`).
>
> 4. **2026-09-01 — default de `asOf` do AgingPanel derivado em UTC — Nível 1 (borda de entrada,
>    classe `date-only-utc-shift`, mesma do `lead360-dateonly-utc-shift`).** `today()`
>    (`AgingPanel.tsx:16-18`) usa `toISOString()` e `generate()` sempre envia `asOf`; entre 21h-00h BRT
>    a posição default pedida é o "amanhã" do escopo e o backend (já corrigido para si via `scopeToday`)
>    responde `as_of_not_today`, suprimindo o tie-out exatamente na posição default. Mapeado pelo dono
>    (mensagem 2026-09-01); adjacência Nível 3 declarada (o FE ignora o contrato `asOf?` opcional), mas
>    a natureza do defeito é derivação de data no fuso errado — conta como Nível 1 para a aposta.
>    **Instrumentado vermelho** em 2026-09-01: `my-app/features/accounting/components/__tests__/AgingPanel.test.tsx`,
>    caso "guarda: default de asOf na janela 21h-00h BRT pede o hoje do escopo, não o amanhã UTC"
>    (instante fixado por fake timers — determinístico fora da janela). Fork (a) — omitir `asOf`
>    enquanto o campo não for tocado (backend decide via `scopeToday`), input preenchido com
>    `report.asOf` — ratificado pelo dono e **FECHADO 2026-09-01** no mesmo dia (ciclo
>    instrumentação→correção; teste-guarda verde, 183/183, `tsc` limpo).
> 5. **2026-09-01 — varredura da classe `date-only-utc-shift` no frontend — Nível 1 (borda de
>    entrada; mesma classe do nº 4/AgingPanel, FECHADO no PR #250, mergeado em 2026-09-02).** Mapeada pelo dono (mensagem 2026-09-01, a mesma que fechou o
>    AgingPanel). 13 sites confirmados que mordem na janela 21h-00h BRT, todos derivando default
>    de data via `toISOString()` (UTC): **5 read-paths** cujo DTO exige a data e o backend não tem
>    default próprio — BP, DRE e DFC (nos 3 o dano cresce em 31/12: a janela year-to-date vira o
>    ano SEGUINTE, relatório vazio), comparativo (`asOfCurrent`) e Livro Diário (`Até`) — e
>    **8 write-paths** que gravam o dia errado em silêncio (verificado: nenhuma checagem de hoje
>    no backend fora do `as_of_not_today` do aging): data do lançamento manual (JournalEntryModal),
>    do estorno (JournalEntriesPanel), da baixa AP e AR (`actionDate`, re-derivada no clique),
>    emissão+vencimento dos 2 modais de criação, data da venda (`useSalesWizard` → ponte contábil
>    reconhece receita no dia errado) e o botão "Hoje" do DynamicForm (rótulo promete hoje, grava
>    amanhã). **Instrumentado vermelho em 2026-09-01**: 13 testes-guarda "guarda: … janela
>    21h-00h BRT …" (instante fixado por fake timers, determinístico — armadilha
>    teste-de-hoje-quebra-em-janela-utc), suíte 182 verdes pré-existentes + 13 vermelhos pela
>    asserção final; em `my-app/features/accounting/components/__tests__/` (BalanceSheetPanel,
>    IncomeStatementPanel, Nucleo4Panels ×3, JournalEntryModal, JournalEntriesPanel,
>    AccountsPayablePanel, AccountsReceivablePanel, CreatePayableReceivableModal.echo ×2) +
>    `features/dashboard/category-views/finance/hooks/sales/__tests__/useSalesWizard.dateDefault.test.ts`
>    + `features/dashboard/components/forms/__tests__/DynamicForm.todayButton.test.tsx`.
>    **Não mordem:** seeds de dev (fixture, datas relativas intencionais);
>    `EditRecordButton.tsx:48` é classe ADJACENTE (rendering round-trip via `new Date(raw)`,
>    morde só se o valor armazenado for datetime — não instrumentado nesta varredura).
>    **Forks abertos e RATIFICADOS pelo dono em 2026-09-02** (`docs/accounting/FE-FIX-DATEONLY-UTC-brief.md`):
>    **F1(a)** read-paths espelham o #250 — DTO `asOf` opcional + default `scopeDay(scope)` no Service;
>    **F2(b)** write-paths corrigem no FE (omitir não é simétrico: `dueDate` não tem default legítimo);
>    **F3(b)** o FE deriva com a constante espelhada de `AccountingScope.timeZone`, NÃO com o
>    `x-user-timezone` (que o `middleware/auth.ts` declara sem autoridade); **F4(b)** o botão "Hoje"
>    do DynamicForm SAI do lote (blast radius é o dashboard, não a contabilidade) e vira item próprio,
>    levando junto o teste-guarda dele — o lote da correção é de **12 sites**; **F5(a)** #250 primeiro,
>    rebase por cima, os dois registros mantidos (esta resolução).
>
>    **CORRIGIDO 2026-09-02** — os 12 sites do lote (13 menos o DynamicForm, que F4(b) tirou para item
>    próprio): BP/DRE/DFC pelo backend (`asOf` opcional + `scopeToday` no Service) e os 9 restantes no
>    FE. Suíte 210/210.
>    **Defeito do próprio instrumento, achado na correção:** 4 dos 13 guardas
>    (JournalEntryModal, JournalEntriesPanel, CreatePayableModal, CreateReceivableModal) consultavam
>    `container.querySelector('input[type="date"]')`, mas o `Modal` renderiza via
>    `createPortal(..., document.body)` — achavam ZERO elementos e estouravam `TypeError` **antes de
>    asserir**. Pareciam "vermelhos pelo motivo certo" e nunca testaram data nenhuma; verificado
>    rodando-os contra o componente pré-fix de `c8314d2e`. Consertados na sessão de instrumentação de
>    2026-09-02: consulta ao `document` + sanidade de existência do input (é a asserção que teria
>    pegado o defeito). Par vermelho→verde provado no mesmo PR — contra os componentes de
>    `c8314d2e` os 4 falham pela ASSERÇÃO (`expected [ '', '2026-08-31' ] to include '2026-09-01'`).
>    **Classe:** `comentario-de-teste-afirma-o-que-nao-assere` — guarda cuja consulta não alcança onde
>    o componente renderiza passa por vermelho legítimo. Quem escreve guarda de componente em portal
>    consulta o `document`, e deixa uma sanidade de existência para que a falha de alcance apareça.
>
> **Viés a declarar:** a entrada nº 3 é classificada como Nível 3, que é exatamente o que a aposta
> acima prevê — classificação feita pela natureza do defeito (regra declarada × caminho real), não
> para favorecer a aposta, mas o leitor deve saber que quem classificou conhecia a aposta.

## Fila corrigida (ordem por valor ÷ atrito, com a regra permanente aplicada)

1. ~~Script do smoke-migration-gate~~ → **Fase 2 deste plano** (13 relatórios já são a spec; lê o app).
2. ~~Harness de concorrência noOverlap~~ → **Fase 3** (`it.failing` expõe o TOCTOU; conserto é decisão à parte).
3. ~~Snapshot de shape Zod~~ → **Fase 4** (evolução assimétrica server-side; sem dep nova, sem passo de CI).
4. ~~Observabilidade (transversal)~~ → **decidido pelo dono em 2026-08-12: F1→(a)**, sink de arquivo
   NDJSON + `npm run logs:errors`. Zero dependência nova, zero dado saindo do processo, sem acionar o
   gate de LGPD. Razão de peso contra (b) Sentry: **o app nunca foi implantado** — alerta remoto compra
   uma produção que não existe, e forçaria hoje a decisão de privacidade que o Bloco A lista como
   oráculo aberto. **O que continua aberto:** agrega mas não *alerta*; alguém tem de rodar o comando.
   Se houver data de implantação, a razão contra (b) cai e ele volta à fila.
5. Censo AV / detector de regra sem lastro — **SUSPENSO** pela regra permanente até um oráculo do
   Bloco A fechar.
6. Estado inconsistente intra-módulo — menor prioridade (1 caso no corpus, já consertado no #176-adjacente).
7. **`unique`/`compositeUnique` sem gate in-tx** (Nível 4, 2026-09-21) — ✅ instrumentado 2026-09-23 (`UniqueFieldConcurrency.integration.test.ts`) e **FECHADO 2026-09-23** (`claude/gapmap7-correcao`): lock `withTableWriteLock` armado também para `unique`/`compositeUnique` + re-check in-tx de `validateAdvancedRules`; `it.failing` → `it`. Não é aparato novo: é a mesma Fase 3 aplicada à regra vizinha. Beneficia CRM/vendas, não só contabilidade.
8. **`deleteTableData` × `immutableAfter`/`lifecycle`** (Nível 3, 2026-09-21) — instrumentar; o fix tem fork (guard no delete × RESTRICT no pai) que é decisão do dono.
9. **Cabeçalho `atomicUntil` + boundary test** (Nível 3; decisão 2026-09-21, fila aberta 2026-09-22) — PR-B, só com "executa": teste jest da mesma classe do `no-accounting-imports.boundary.test.ts` (população calculada por `grep .postEntry(`, sem registro a manter) + retrofit dos 8 cabeçalhos, vermelho→verde no mesmo PR (`protocolo-conserto-de-gate`). Não é aparato de auditoria (regra permanente): é guarda de repo que roda no `Server – typecheck & test`. A primitiva `commitThenReconcile` **não** entra na fila — nasce de incidente (`ADR-DOMAIN-MOTOR-rejected.md` §3).
10. **C-1 — CST 04..09 decide antes do NCM** (Nível 5, 2026-09-25; triagem do contador P5, `TRIAGEM-RESPOSTA-CONTADOR-2026-09-23.md`) — `classifyPisCofinsItem` (`pisCofinsMonofasicoNcm.ts:119-120`) devolve MONOFASICO para NCM fora da tabela + CST 04; esperado TRIBUTADO + alerta de CST divergente. **Instrumentado vermelho 2026-09-25 (PR #379, `f8029916`)**: `server/src/lib/__tests__/nfeCost.test.ts` › "GAP C-1 …" (`it.failing`). Prova: `cd server && npm run test:unit -- src/lib/__tests__/nfeCost.test.ts -t "GAP C-1"`. Correção = passo 1.7 do `PLANO-POS-CONTADOR-2026-09-23.md`, **não autorizada** (F-PC-2). **FECHADO 2026-09-25** (PR #381, `c1937b1a`): o NCM decide; CST 04 com NCM fora → TRIBUTADO + alerta no `warnings` (F-PC-2 a); `it.failing` → `it`. CST 05..09 seguem decidindo pela nota (fora do P5).
11. **C-3 — CST 02 com NCM comum fica sem crédito** (Nível 5, 2026-09-25; triagem item 8) — `pisCofinsMonofasicoNcm.ts:133-134` cai em UNKNOWN; esperado crédito 1,65% + 7,6% sobre base sem ICMS e sem IPI, alerta mantido. **Instrumentado vermelho 2026-09-25 (PR #379, `f8029916`)**: `nfeCost.test.ts` › "GAP C-3 …". Prova: `cd server && npm run test:unit -- src/lib/__tests__/nfeCost.test.ts -t "GAP C-3"`. Correção: passo 1.7, não autorizada. **FECHADO 2026-09-25** (#381): `CST_TRIBUTADO = [01, 02]`, CST 02 credita com alerta; `it.failing` → `it`.
12. **IPI-BASE — IPI entra na base do crédito quando a flag liga** (Nível 5, 2026-09-25; triagem P1, STJ Tema 1.373 — "regra fixa, não parâmetro") — V6: `nfeCost.ts:130` soma `vIPICents` se `pisCofinsCreditIncludesIpi` (default `false`, `schema.prisma:1328`, `FiscalProfileDto.ts:37`, mas o tenant pode ligar). **Instrumentado vermelho 2026-09-25 (PR #379, `f8029916`)**: `nfeCost.test.ts` › "GAP IPI-BASE …". Prova: `cd server && npm run test:unit -- src/lib/__tests__/nfeCost.test.ts -t "GAP IPI-BASE"`. Correção: passo 1.7, não autorizada. **FECHADO 2026-09-25** (#381): `nfeCost.ts` não soma IPI; `UpsertFiscalProfileSchema` rejeita `pisCofinsCreditIncludesIpi=true` (400, decisão do dono 25/09); `it.failing` → `it`.
13. **C-2 — tabela monofásica sem bebidas frias e combustíveis** (Nível 5, 2026-09-25) — `PIS_COFINS_MONOFASICO_NCM` não tem a Lei 13.097/2015 art. 14 nem combustíveis → CST 01 vira TRIBUTADO. **Instrumentado vermelho 2026-09-25 (PR #379, `f8029916`) (só a metade bebidas)**: `server/src/features/accounting/models/__tests__/pisCofinsMonofasicoNcm.test.ts` › "GAP C-2 …" (NCM 2203.00.00, chave `L13097-art14-IV` de `docs/accounting/fontes-oficiais/TRANSCRICAO-monofasico-bebidas-combustiveis-2026-09-25.md`). Prova: `cd server && npm run test:unit -- src/features/accounting/models/__tests__/pisCofinsMonofasicoNcm.test.ts -t "GAP C-2"`. **Bebidas FECHADO 2026-09-25** (#381): Lei 13.097 art. 14 I–IV na tabela; `it.failing` → `it`. **Combustíveis FECHADO 2026-09-27** (autorização do dono: "resolve o combustível no gap-map"): o que faltava não era decisão, era a **fonte** — a Lei 9.718 art. 4º nomeia produtos, e a correspondência produto → NCM vive na **Tabela 4.3.10 da EFD-Contribuições**, que a leitura de 25/09 nomeou mas não conseguiu baixar. Baixada de <http://sped.rfb.gov.br/arquivo/download/1638> (v1.25, 30.03.2026; `.doc` de 293.888 B, sha256 `82441544f8ac…`), texto no corpus como `tabela-4310-efd` e transcrita em §B.1 (só linhas de término VAZIO). Entraram 7 prefixos (2710.12.59 · 2710.19.21 · 2711.19.10 · 2710.19.11 · 3826.00.00 · 2207.10 · 2207.20.1), cada um com a chave `T4310-NNN` na `fonte`. Par vermelho→verde no mesmo PR: o teste novo falhou em `27101259 → TRIBUTADO` antes do fix e passa depois; um 2º caso (negativo) fixa que linha ENCERRADA e `Ex` da TIPI **não** entram (2710.11.59, 3824.90.29, 2208.90.00). A guarda de `fonte` do teste foi estendida para admitir a tabela oficial além das leis. **Resíduo declarado, fora deste GAP:** 105–108 e 150–153 (correntes/nafta) têm NCM `-` na fonte e ficam sem entrada; o crédito do não-varejista pelo valor da nota (Lei 13.097 art. 30) segue não modelado — conservador = sem crédito.
14. **SIG-NFE — assinatura XMLDSig da NF-e não verificada** (Nível O, 2026-09-26; nó `SIG-NFE`, Fase 2 do plano pós-contador) — ✅ **instrumentado vermelho 2026-09-26** (autorização do dono em chat: "instrumenta a assinatura"): `nfeController.purchase.signature.integration.test.ts` (`it.failing`, `Expected: 400 / Received: 201`). Próximo: BRIEF 2.3 + F-SIG-1 (dono), implementação 2.4.

> Ao corrigir 10–12, três asserções de hoje fixam o comportamento antigo e viram obsoletas junto (o fix as troca, com ERRATA — não é regressão): `pisCofinsMonofasicoNcm.test.ts` "classificação (item 11)" (CST 04 com 63026000 → MONOFASICO; CST 02 → UNKNOWN) e `nfeCost.test.ts` "as 3 flags … IPI na base (+300)". **Feito na correção de 2026-09-25**, com comentário `ERRATA 2026-09-25` em cada uma.

## Uso em projeto em andamento (o processo do original, com duas emendas)

O fluxo censo → triagem → instrumentar-antes-de-corrigir → gate de módulo novo continua válido, com:

1. **"Instrumentar antes de corrigir" é regra da casa comprovada** — o teste que falha vem antes do fix
   (foi assim no #176: mordida provada nos dois elos antes do merge). Mantido.
2. **O censo está suspenso** (regra permanente do `CLAUDE.md`) e a triagem por "framework C-D" foi
   removida — o framework não existe neste repositório; a triagem aqui é decisão do dono sobre a lista,
   sem aparato novo.
3. Censo e correção em sessões separadas: mantido — é o mesmo "achado não triado não se conserta" que
   esta casa já praticava.
